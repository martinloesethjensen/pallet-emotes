{
  "name": "pallet-emote-cli",
  "version": "1.0.0",
  "description": "A Cli tool which can be uses to create a mapping between Unicode Emojis and the Bytewise Mapping which is used by the pallet to store date on chain",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
